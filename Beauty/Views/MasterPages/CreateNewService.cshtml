<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        .form-signin {
            width: 100%;
            max-width: 330px;
            padding: 15px;
            margin: auto;
        }

        .body {
            background: url(~/img//back.jpeg);
            background-attachment: fixed;
            background-size: 100% 100%;
        }

        .font-weight-normal {
            font-weight: 400 !important;
        }

        .mr-md-auto, .mx-md-auto {
            margin-right: auto !important;
        }

        .d-flex {
            display: flex !important;
        }

        .container {
            width: 100%;
            padding-right: 15px;
            padding-left: 15px;
            margin-right: auto;
            margin-left: auto;
        }

        div {
            display: block;
        }

        body {
            font-family: 'Muli', sans-serif;
            background: #ECDFD7 !important;
        }
    </style>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="/js/site.js"></script>
    <script src="~/lib/sweetalert.min.js"></script>
    <link rel="stylesheet" href="~/css/bootstrap.min.css" type="text/css" />

</head>
<body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3  border-bottom box-shadow">
        <h5 class="my-0 mr-md-auto font-weight-normal">Beauty</h5>
        <nav class="my-2 my-md-0 mr-md-3">
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="HomePage">Главная</a>
            <a class="btn btn-secondary rounded-pill" asp-controller="MasterPages" asp-action="CreateNewService">Добавить услугу</a>
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="addShedul">Добавить расписание</a>
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="addPhoto">Добавить фото</a>
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="ServiceList">Мои услуги</a>
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="selectHistory">Записи</a>
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="selectMasterInfo">Профиль</a>
            <a class="btn btn-outline-secondary rounded-pill" asp-controller="MasterPages" asp-action="Signout">Выйти</a>
        </nav>
    </div>

    <h2 class="text-center">
        Добавить услугу
    </h2>
    <h4 class="text-center">
        Выберите категорию и заполните поля
    </h4>

    <form style="margin-left: 30%; margin-right: 30%;">
        <div class="col-md-5 mb-3">
            <label style="margin-top:20px; margin-bottom:5px;">Выберите услугу</label>
            <select class="form-control rounded-pill" name="catg" id="category" required="" onchange="getSubcategory(this)">
                <option>Выберите</option>
                @foreach (var category in Model.categories)
                {
                    <option value="@category.Id">@category.CategoryName</option>
                }
            </select>
        </div>
        <input type="text" class="form-control mb-3 rounded-pill" placeholder="Цена" required="" name="price" id="price">
        <textarea type="text" class="form-control mb-3 rounded-pill" placeholder="Описание(до 100 символов)" required="" name="discription" id="discription" maxlength="100"></textarea>
        <button class="btn btn-secondary rounded-pill" type="button" style="margin-left: 83%;" onclick="addService()">Добавить</button>
    </form>

</body>
</html>

